#[macro_export]
macro_rules! gate {
    ($pool:expr, $req:expr, $permission:expr) => {
        // Minimal stub for gate macro
        // In a real implementation, this would check if the user has the permission
    };
}
